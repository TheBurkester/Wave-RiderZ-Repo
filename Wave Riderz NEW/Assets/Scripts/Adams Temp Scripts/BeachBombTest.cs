using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BeachBombTest : MonoBehaviour
{
    public Animator PlaneAnim;

    /*

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKey(KeyCode.A))
        {
            PlaneAnim.SetBool("FireBeachBomb", true);
            Debug.Log("you got it!");
        }
        if (Input.GetKey(KeyCode.Q))
        {
            PlaneAnim.SetBool("FireBeachBomb", false);
            Debug.Log("Im done");
        }
        if (Input.GetKey(KeyCode.D))
        {
            PlaneAnim.SetBool("OpenMine", true);
            Debug.Log("All Mine!");
        }
        if (Input.GetKey(KeyCode.C))
        { 
            PlaneAnim.SetBool("OpenMine", false);
            Debug.Log("Closed");
        }
    }
    */
}
